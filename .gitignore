# --- Python ---
venv/
# Entorno de Python creado en esta máquina: el `venv/` del repositorio
# apunta a una ruta que aquí no existe (otra máquina, otro usuario).
venv312/
__pycache__/
*.pyc
*.pyo

# --- Node / React ---
frontend/node_modules/
frontend/dist/

# --- Builds de escritorio (PyInstaller) ---
build/
dist/
*.spec.bak

# --- Configuracion local (subir solo .env.example) ---
.env

# --- Otros ---
nohup.out
*.log
.vscode/
.idea/

# --- Datos locales del backend (conexiones a BD + clave de cifrado) ---
datos/

# --- Archivos que deja la app de escritorio de Claude (pantallazos, etc.) ---
Claude outputs/
